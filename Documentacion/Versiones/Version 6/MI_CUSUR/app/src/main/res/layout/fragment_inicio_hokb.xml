<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Fragment_Inicio_HOKB"
    android:background="@color/colorFondoContenido"
    android:orientation="vertical">

    <!-- TODO: Update blank fragment layout -->


    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="Titulo"
        android:id="@+id/titulo_inicio_consulta_hokb"
        android:layout_gravity="center_horizontal"
        android:textAlignment="center"
        android:background="@color/colorFondoEncabezado"
        android:textColor="@color/colorTextoEncabezado" />

        <ListView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/lista_inicio_consulta_hokb"
            android:layout_below="@+id/titulo_inicio_consulta_hokb"
            android:layout_above="@+id/total_inicio_orden_pago"
            android:paddingTop="8dp"
            android:scrollbarStyle="outsideOverlay"
            android:divider="@android:color/transparent"
            android:clickable="false"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Total"
        android:textColor="@color/colorTextoEncabezado"
        android:textAlignment="center"
        android:id="@+id/total_inicio_orden_pago"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:background="@color/colorFondoEncabezado"
        android:visibility="invisible" />


</RelativeLayout>
